Configuration Directory

This directory contains the central configuration files for the project.

- config.php: Master include file that defines global constants.
- database.php: Returns the database configuration array.
- app.php: Returns the application settings like URLs and SMTP credentials.

Note: Direct access to this directory is blocked via .htaccess for security reasons.
